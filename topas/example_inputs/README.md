# Example TOPAS Inputs

No complete TOPAS simulation inputs are included in this curated public repository.

This directory is reserved for a future small, non-private example input if one is needed to document file format conventions. The repository is intended to publish processed figure-source data rather than the full Monte Carlo workflow or large raw dose arrays.
